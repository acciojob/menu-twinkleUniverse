/*import React from "react";

const Home = ({setSelection}) => {
   

    return (
        <div>
            <h1>Our Menu</h1>
            <nav>
                <h3 onClick={()=>setSelection(1)}>All</h3>
                <h3 id="filter-btn-1" onClick={()=>setSelection(2)}>Breakfast</h3>
                <h3 id="filter-btn-2" onClick={()=>setSelection(3)}>Lunch</h3>
                <h3 id="filter-btn-3" onClick={()=>setSelection(4)}>Shakes</h3>
            </nav>
        </div>
    )
};

export default Home;*/